# xGEOID20B Batch Extraction Tool
# Python dependencies
# Install with: pip install -r requirements.txt
# (conda is preferred - see environment.yml)

netCDF4>=1.6.0
numpy>=1.23.0
# Only needed for the optional 'xlsx' output format.
openpyxl>=3.0.0